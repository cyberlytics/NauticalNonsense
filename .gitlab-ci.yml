include:
  - local: sys_src/templates/terraaform.gitlab-ci.yml
    rules:
      - if: $CI_PIPELINE_SOURCE == "web"
  - local: sys_src/templates/python.gitlab-ci.yml
    rules:
      - if: $CI_PIPELINE_SOURCE == "push"